# Functions = A block of reusable code
#             use () after a function to activate it or Invoke it or call it

# any data you send to function are called as arguments
# you need matching parameters to accept those arguments
# Return = return is a statement that is used to end a function and send the result back to caller

a = int(input("Enter your age :"))
def Age(a):
  print(f"Your age is {a}")
  # or return "your age is" + a
Age(a)  
  
  
