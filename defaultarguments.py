# Default arguments = a default value of a certain parameters 
#                     default is used when that argument is omitted
#                     make your function more flexible ,reduction of arguments 

def add(a=2,b=2):
  c = a+b
  print(c)
add()  
# we can add our own arguments but defaults are now a = 2 and b = 2  
# in keyword arguments there is a function called sep
# it separates given arguments by a anything
